#include <stdio.h>
#include <stdlib.h>
#include "arvore.h"
#include "aarvore.h"
int main()
{
    // printf("%.2f", num); //apenas se for usar em C puro, não use em C++



    controleFuncionario();

   /*
    Aarv* a1= cria('d',inicializa(),inicializa()); // sub-árvore com 'd'
    Aarv* a2= cria('b',inicializa(),a1); // sub-árvore com 'b'
    Aarv* a3= cria('e',inicializa(),inicializa()); // sub-árvore com 'e'

    Aarv* a4= cria('f',inicializa(),inicializa()); // sub-árvore com 'f'
    Aarv* a5= cria('c',a3,a4); // sub-árvore com 'c'
    Aarv* a = cria('a',a2,a5 ); // árvore com raiz 'a'

    imprime(a5);

    libera(a1);
    libera(a2);
    libera(a3);
    libera(a4);
    libera(a5);
    libera(a);
*/
    return 0;
}

