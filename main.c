#include <stdio.h>
#include <stdlib.h>
#include "arvore.h"
int main()
{
    // printf("%.2f", num); //apenas se for usar em C puro, não use em C++

    float a;
    printf("Hello world!\n");

    controleFuncionario();
    return 0;
}
